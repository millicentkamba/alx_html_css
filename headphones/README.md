This project is part of the ALX Front-End specialization.
The goal is to recreate a fully responsive webpage for a headphones brand using HTML and CSS only — no frameworks, no libraries.

📋 Project Overview

The project focuses on building a clean, well-structured webpage using best practices:

✔ Clean HTML structure

semantic tags

simple container-based layout

minimal and generic selectors

✔ Clean CSS structure

reset CSS

use of variables

no overly specific selectors

responsive layout (desktop + mobile)

background images controlled with CSS